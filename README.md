# blog-examples

Companion code for posts on [matlog.dev](https://matlog.dev).

Each example lives in its own subfolder under the year it was published.

## Structure

```
2026/
  agent-observability/   # contract_review_agent.ipynb — dspy.ReAct + MLflow tracing
```

## Usage

Each subfolder has its own `README.md` with setup instructions.
Dependencies are managed with `uv` (`pyproject.toml` / `uv.lock` per project).

