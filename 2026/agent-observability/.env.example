# Provider API Setup: I use OpenRouter — see https://openrouter.ai/
PROVIDER_API_KEY=
PROVIDER_ENDPOINT=
MODEL=

# MLflow — start server with: uvx mlflow server --port 5001
MLFLOW_TRACKING_URI=http://localhost:5001

# Brave Search API — free tier at https://brave.com/search/api/
BRAVE_API_KEY=
